Identify the next task to work on:

1. Read the "Open items" section at the top of `docs/STATE.md`
2. Pick the highest-value item that isn't blocked (deliberate cuts listed there are *not* candidates)
3. Read ONLY the specific spec sections referenced for that item (not the full docs)
4. Propose a concrete implementation plan with small, committable steps

Keep the plan short — max 5 steps. Each step should result in one conventional commit with green tests, and the final step updates `docs/STATE.md`.
