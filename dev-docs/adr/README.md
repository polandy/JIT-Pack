# Architecture Decision Records

One file per decision, named `ADR-00N_Short_Title.md`, numbered in the order the decision was *taken*. An ADR is never
rewritten to say something else — when a later decision overrides it, the old record gets a `Status: Superseded by
ADR-00M` line and stays.

## Index

| # | Decision | Status |
|---|---|---|
| [001](ADR-001_v2_Stack_Sync.md) | Stack & sync architecture — Go + embedded SQLite (v2, replacing central PostgreSQL) | Accepted |
| [002](ADR-002_Avatar_Storage.md) | Avatar storage — database BLOB vs. filesystem | Accepted |
| [003](ADR-003_Conflict_Resolution.md) | Conflict resolution strategy | Accepted |
| [004](ADR-004_Auth_Strategy.md) | Authentication strategy | Accepted |
| [005](ADR-005_Push_Notifications.md) | Push notifications | Accepted |
| [006](ADR-006_Client_Framework.md) | Client framework | Accepted |
| [007](ADR-007_Session_Brokering.md) | First-party sessions brokered from the IdP | Accepted |
| [008](ADR-008_Client_Side_Generation.md) | Generation and import run on the client, not the server | Accepted |
| [009](ADR-009_Three_Run_Modes.md) | Three run modes from one artifact | Accepted |
| [010](ADR-010_CSV_Only_Import.md) | Spreadsheet import accepts CSV only, not XLSX | Accepted |
| [011](ADR-011_One_Header_Bar.md) | One header bar whose left slot switches, rather than a global bar plus per-screen headers | Accepted |
| [012](ADR-012_One_Router_Outlet.md) | One router outlet, rather than Ionic's nested `IonTabs` outlet | Accepted |
| [013](ADR-013_Visual_Baselines.md) | Visual baselines render in a digest-pinned container, not on the CI runner | Accepted |
| [014](ADR-014_Item_Tags.md) | An item carries a set of tags in a join table; the category is renamed away, not kept beside it | Accepted |
| [015](ADR-015_Local_Backup_File_Shape.md) | The Local Mode backup is one multi-document YAML file, not one file per trip | Accepted |
| [016](ADR-016_Group_Refresh_Ledger.md) | The group refresh keeps a ledger — for what it wrote, and for what was refused | Accepted |
| [017](ADR-017_Portable_Composition.md) | A portable Ferien-Vorlage carries its groups whole | Accepted |
| [018](ADR-018_No_DDL_Migrations_In_Development.md) | No DDL migrations during the development phase — one always-current `schema.sql` | Accepted; revisit trigger fired, see [067](ADR-067_The_Store_Gets_A_Migration_Path_Inside_0x.md) |
| [019](ADR-019_App_Shell_Service_Worker.md) | App-shell caching is hand-rolled in the existing worker, not vite-plugin-pwa | Accepted |
| [020](ADR-020_Touch_First_Adding.md) | Touch-first adding — chip rows now (FR-25.13c), inventory browse-sheet as the decided next stage (FR-25.13d) | Accepted |
| [021](ADR-021_Item_Mark_Emoji.md) | One optional emoji per item and template as the packing row's scan aid, from a self-hosted subset — over an icon library, photo-only and an initial tile (§3.28, G-15) | Accepted |
| [022](ADR-022_Per_Field_Clocks.md) | The merge compares each field against its own persisted clock (`field_hlcs` JSON column) — over a side table and over the row-level clock the code had — and rule 2 is the two state pairs §6 names (NFR-4.2a) | Accepted |
| [023](ADR-023_Conflict_Revert_As_New_Mutation.md) | A manual conflict revert is an ordinary mutation with a fresh server HLC — over rewriting the row in place, a client-side re-issue, and leaving the log read-only (NFR-4.2a) | Accepted |
| [024](ADR-024_Portable_Restores_What_It_Saved.md) | A portable file restores what it saved — trip status, item marks and ordered tags in one format — over a backup-only dialect and over honouring them on the restore path alone (FR-18.2–18.5, NFR-4.11) | Accepted |
| [025](ADR-025_One_Importer.md) | One implementation of the portable format: the server's reader *and* writer are deleted and the CLI runs the client's, over porting them to Go behind a shared conformance corpus (ADR-008, FR-18.7) | Accepted |
| [026](ADR-026_Generated_Wire_Contract.md) | The client/server contract is declared once in Go and the client's types are generated from it with a CI gate, over a hand-kept OpenAPI file or a compare-only gate (NFR-4.14) | Accepted |
| [027](ADR-027_Route_Shapes.md) | A route names its scope first, then the resource — the master partition's scope segment is `master` and an export names its format — over a collection-first shape that would exempt the sync channel (NFR-4.14) | Accepted |
| [028](ADR-028_Claims_End_By_Decision.md) | A packing claim ends by decision — taken over by a named person, notified and recorded — over expiring on a clock, or expiring with an announcement, which costs the same and still lets a duration overrule a person (FR-5.7/FR-5.3) | Accepted |
| [029](ADR-029_Mock_IdP_For_Multi_Identity_E2E.md) | The e2e suite gets its second account from a **mock IdP fixture** driven through the app's real login, over running a real Authelia container (a third pinned toolchain plus a provider's configuration surface) or a test-only auth bypass in the shipped binary (UI-Test-Spec §2.3) | Accepted |
| [030](ADR-030_Trip_Identity_On_Import.md) | An imported document is a second copy when the **name** matches (plus the **year**, for a trip), decided in the client and reported per document, over a UNIQUE constraint (which parks the outbox), a `(import)` suffix (which labels duplication rather than preventing it) or a merge. Retires ADR-017's suffix for Ferien-Vorlagen (FR-18.4) | Accepted |
| [031](ADR-031_Refusal_Repairs_The_Row.md) | A refused mutation repairs the row it refused — the server re-logs it so the ordinary pull carries the truth, and the client drops the one row the server cannot re-log without leaking it — over a client-side undo, a second read path for one entity, or a whole-partition resync (Sync-API §5, P-1/P-3) | Accepted |
| [032](ADR-032_Retire_Decided_Twice.md) | FR-24.3's retire-or-remove decision runs **on the server, authoritatively, and on the client, advisorily** — over one rule reached through a new usage endpoint, or one rule on the client with the server merely refusing; and `itemList`/`templateList` keep meaning everything while display surfaces opt in to the active lists (FR-24.3, invariants 4/5) | Accepted |
| [033](ADR-033_Trip_Progress_On_The_List.md) | M2 loads the trip partitions of the rows that are **on screen** and says *unknown* until they arrive, over loading every trip's partition on mount (measured 1.1 MB for one archive) or carrying the counts on the trip row (a derived aggregate under LWW) (FR-2.3) | Accepted |
| [034](ADR-034_Restore_Makes_Room_For_Itself.md) | A restore whose name was taken while the row was retired **makes room for itself** — the refusal carries a replacement name, written in the *same* mutation as the cleared marker — over refusing and stopping (which dead-ends the case the partial index was chosen to allow), merging into the holder (whose references live in trip partitions the device need not hold), or letting the push refuse it (Local Mode has no guard, and elsewhere ADR-031 reverses the write in front of the user) (FR-24.3, M23) | Accepted |
| [035](ADR-035_Form_Controls_Wear_The_Theme.md) | Date and file inputs are **themed Ionic controls**, not the browser's native ones — over living with a control that ignores the token tables and speaks the OS language rather than the app's (UX-6, NFR-4.12, invariants 9/9b, G-17) | Accepted |
| [036](ADR-036_Membership_Rewrites_Rows.md) | Editing who needs an item **keeps the rows it can and repoints them** (ids derived per ADR-016), over delete-and-recreate (which throws away packing state) or a membership table (a schema change for a relation the rows already express) (FR-25.21) | Accepted |
| [037](ADR-037_The_Worker_Reads_The_Language.md) | The service worker learns the language from an **IndexedDB mirror** the app writes, over posting the locale to it (gone the moment a push wakes it cold) or having the server render the text (which it cannot, and which would hold a second copy of the vocabulary) (NFR-4.12, NFR-4.6) | Accepted |
| [038](ADR-038_A_Master_Row_Has_A_Delete_Endpoint.md) | A master row has a delete endpoint, and the app deliberately does not use it | Accepted |
| [039](ADR-039_Unmanaged_API_Tokens.md) | An API token is an unmanaged JWT — listing and single revocation given up on purpose | Accepted |
| [040](ADR-040_Formatting_Is_Checked_Not_Applied.md) | Formatting is checked, not applied — the bot stops pushing to the branch | Accepted |
| [041](ADR-041_Grid_Preview_Shows_The_Grid.md) | M15's parser preview shows **the grid itself**, over a summary of what it found (FR-16.2) | Accepted |
| [042](ADR-042_Automation_Runs_The_Command_Line.md) | Automation reaches the app through **`jitpack COMMAND`** over the client's own rules, over per-entity REST resources (which would need every rule a second time in Go) or documenting the raw sync push (FR-18.7/18.8) | Accepted |
| [043](ADR-043_One_Container_Serves_Both.md) | **One container serves the client and the API** from `JITPACK_WEB_ROOT`, over embedding the bundle in the binary (which would break `go build` in a fresh clone) or keeping the SPA-plus-nginx image (which left the same-origin rule to the operator, and it failed silently) | Accepted |
| [044](ADR-044_An_Update_The_User_Asked_For.md) | A waiting PWA version is applied **on a press** — `skipWaiting()` reached from a message, never from `install` — over automatic takeover (reloads unasked) or announce-only (a reader and no verb) (FR-19.7, NFR-4.13) | Accepted |
| [045](ADR-045_Leaving_Local_Mode_Through_The_Backup_File.md) | Leaving Local Mode is a **guarded three-step move on M17 through the backup file**, over device-to-device (needs a second phone), a native replay of the local store as sync mutations (a second writer of the feed) or a guarded toggle (strands by design) (FR-19.8, FR-19.5) | Accepted |
| [046](ADR-046_The_Item_Is_A_Query_On_The_Trip_Route.md) | The item over the packing list is **`?item=` on the trip's own route** — over the path alias (to Ionic a second page, mounted on every open and seen as two live M4s on a loaded WebKit runner), local state (no deep link, no reload, no back) or a patched matcher (UI-Spec M5, G-4, E2E-M5-12) | Accepted |
| [047](ADR-047_One_Identity_Read_Per_Session.md) | The user directory and `me` are read **once per session**, in a pinia store, and re-read by the four writers that change them — over a fetch per screen (nine of them, stale-proof only by accident), a time-to-live (right *probably*) or a module singleton (no per-test reset) (FR-4.5, FR-23.3, E2E-M20-07) | Accepted |
| [048](ADR-048_Own_Palette_Over_Catppuccin.md) | The app paints its **own palette, *Bergluft*** (Nacht/Tag), in the same token table and behind the same roles and gate — over keeping Catppuccin and fixing everything around it (a tidier editor theme) or adopting a published tonal scale (a different token shape) (FR-21.2, FR-21.7, G-11, invariant 9) | Accepted |
| [049](ADR-049_One_Ionic_Mode_And_The_Controls_Told_Once.md) | The client runs in **one Ionic mode (`md`) on every platform**, and the controls Material would shape — button, segment, checkbox, header bar, active tab — are told once in the token tables; over pinning `ios` (a rewrite of the test surface for defaults that are as foreign) or leaving the mode to detection and restyling both (two looks, one of them never rendered) (G-9, G-13, G-14, invariant 9b) | Accepted |
| [050](ADR-050_The_Page_Names_Itself.md) | **The page names itself**: `PageHead` renders once in the frame, above the outlet and inside the content column, from the head each screen registers — a display-role title plus an optional meta line — and the app bar renders no page title; its per-page glyph cluster is capped at three, the surplus becoming words in the ⋮ (M4's shopping/luggage/analytics). Over a head per view (26 sites, silent when one is forgotten) and over restyling the bar's title (the size a bar can give is the constraint that failed). Accepted cost: a fixed head that does not scroll, and §3.25's "one tap each" spent (G-9, G-12, G-13, FR-21.10) | Accepted |
| [051](ADR-051_Trip_View_Switcher.md) | **The trip's views are a switcher in the page**: pills inside the page head, rendered once by the frame from `meta.tripView`, the current one marked and inert — over promoting one view back onto the bar (answers driver 1 for one of three, and re-opens the argument the three-glyph budget settled) and over a trip-level tab bar (a second anchor model, 56 px that never collapse). Pays back the §3.25 cost ADR-050 wrote down, and adds the sideways step the ⋮ never had. Accepted cost: ~35 px of head, glyphs only from 480 px up, and `App.vue` importing one trips component. **Amendment 1 (2026-09-20)**: only the views a trip is *worked* in earn a pill — *Packliste*, *Einkaufen (n)*, plus the current view where it is neither — and *Gepäck*/*Auswertung* are ⋮ entries the frame contributes, ahead of what changes the trip; a row of four made a screen read once a trip as loud as the list being packed (G-9, G-12, FR-21.21, FR-25.6) | Accepted |
| [052](ADR-052_The_Tombstone_Outranks_A_Stale_Write.md) | **A deleted row stays deleted**: where a mutation finds no row the store reads the newest tombstone in that feed and refuses the write as `row_deleted` unless its clock is strictly newer — over a `deleted` column per table (the purest merge fit, but invariant 2 prices it at reseeding every database) and over refusing every write to a deleted id (breaks undo and FR-24.3's restore). Accepted cost: an unindexed `change_log` lookup for every write that finds no row, and precedence stated in two files (NFR-4.2a, Sync-API §5/§6, ADR-031) | Accepted |
| [053](ADR-053_An_Empty_Roster_Reports_Rather_Than_Swallows.md) | **A per-person position with nobody to belong to is reported, not swallowed**: generation returns the positions an empty roster could not place, M3's preview names them and the step to fix them, and FR-27.10 answers "needs a traveller" instead of "contributes nothing" — over forbidding an empty roster in step 2 (removes a state the product supports, and reaches only one of three generation paths) and over falling back to trip-global (silently demotes four toothbrushes to one, and becomes a second implicit writer of ADR-036's transition). Accepted cost: a report rather than a gate, and FR-27.4's refresh deliberately excepted (FR-1.4, FR-2.5a, FR-27.10) | Accepted |
| [054](ADR-054_For_Everyone_Is_A_Row_Verb.md) | **A third verb on the browse-sheet line, „Für alle“**: one tap gives every traveller a row at amount one, keeping an amount anybody chose (ADR-036 keep-and-repoint), with no editor opening so a run stays in the sheet — over a *Gemeinsam / Pro Person* mode in the sheet head (a mode that can be silently wrong, the reason FR-25.13f rejected one), over unfolding the roster inside the line (reorders the list under the finger, and rebuilds the membership editor’s picker) and over offering it after an ordinary add (two taps for the common case). Accepted cost: a third square beside the name, and no spread on a line the run itself just added (FR-25.13g, FR-25.21c, G-8) | Accepted |
| [055](ADR-055_Shutdown_Waits_For_The_Delivery_It_Started.md) | **A detached Web Push delivery is waited for at shutdown**: the Server counts the goroutines it detaches, `WaitDetached(ctx)` drains them, and `cmd/jitpackd` calls it after the HTTP drain on the deadline it already had — over a completion hook only a test would set (a seam with one caller, and the shutdown hole stays open) and over sending Web Push inside the request (puts a third-party outage on the sync path, which is why the send was detached). Accepted cost: stopping an instance whose push service is unreachable takes the full five seconds, and "call it only once requests have stopped" is a doc comment rather than a check (NFR-4.6, FR-6.2) | Accepted |
| [056](ADR-056_A_Subscription_Is_Authorised_On_Every_Send.md) | **The WebSocket hub authorises every send, not just the subscribe frame**: `Hub` takes a `ReceiveFunc` and filters the subscribed connections through it before building any event or presence payload, the server answering it as “the account is active and the user is still a member” through the `identity` so Single-User Mode needs no branch — over a `Hub.DropSubscriber` called from the revocation path (free at broadcast time, but it is a list of call sites that was incomplete the day it was written, and a forgotten one reopens the leak silently). Accepted cost: two indexed lookups per subscribed connection per broadcast, and the presence roster keeps a revoked member until the next presence event (FR-4.7, FR-23.3, Sync-API §7) | Accepted |
| [057](ADR-057_A_Broadcast_Waits_For_No_Peer.md) | **A WebSocket broadcast never waits for one peer**: each connection owns a bounded frame queue and a single writer goroutine, and a connection whose queue overruns is closed rather than waited for — over one goroutine per send (unbounded parked goroutines, and the library refuses concurrent writers anyway) and over merely shortening the 5 s write deadline (makes the shared delay smaller, and still grows it with every stalled device). Accepted cost: a slow-but-live peer is disconnected and pays a reconnect and a full re-pull, and there is one goroutine per open connection (Sync-API §7, P-1) | Accepted |
| [058](ADR-058_A_Linked_Traveler_Notifies_Through_Membership.md) | **A linked traveler notifies through membership**: the link alone carries no reach — require it, rather than granting membership silently or skipping the notification (FR-2.5, FR-6.2) | Accepted |
| [059](ADR-059_A_Refused_Refresh_Ends_The_Session_An_Outage_Does_Not.md) | **A refused refresh ends the session, an outage does not**: only what was never answered is retried, so an IdP hiccup cannot log a household out — and a refusal is not retried into a loop (FR-19.6, FR-19.9, NFR-4.4) | Accepted |
| [060](ADR-060_A_Banner_Never_Moves_The_Page_Under_A_Finger.md) | **A banner never moves the page under a finger**: the FR-19.7 update offer renders in its own fixed layer over the content — over reserving the space permanently and over a bottom shelf, which relocates the moving targets instead of removing them (FR-19.7, G-19) | Accepted |
| [061](ADR-061_The_Tag_Axis_Was_Navigation_Not_A_Filter.md) | **The inventory's tag axis was navigation, not a filter**: M9's scrollable segment becomes three counted chips, a searchable multi-select sheet and a jumping group heading — over a wrapping chip cloud (degrades as the vocabulary grows) and an anchored dropdown (forecloses multi-select). Accepted cost: the sort moves into the G-12 cluster, and two mechanisms — jumping and filtering — sit side by side (FR-24.8, UI-Spec M9) | Accepted |
| [064](ADR-064_A_Detail_Pane_Belongs_To_The_Frame.md) | **A detail pane belongs to the frame, not to the screen**: `.app-body` gains a third flex column that collapses when empty, and M5's pane is teleported into it, so the two panes cannot overlap and the pane reaches the window's edge without positioning — over anchoring the layer to the window (still covers 100 px at 1280, ~290 px at 900) and over keeping the overlay and correcting the spec (blesses a 200 px strip of bare item names). Accepted cost: the column re-centres when the pane opens, 200 px at 1280, and a page-scoped locator no longer finds the pane (G-9, M5, `--jp-panel-w`) | Accepted |
| [065](ADR-065_A_Removal_Prunes_Its_Item_Once_Final_And_Only_Where_Unused.md) | **A removal prunes its item once final, and only where nothing uses it**: FR-5.8's removal deletes the inventory item it leaves unused when the undo lapses (or at once after a confirmation), a server device through `POST /master/items/{id}/prune`, which keeps a used item untouched — over deleting at once through the push (the master queue drains first, so the delete meets the row it follows and FR-24.3 retires the item every time, and the undo cannot bring back a photo) and over a `prune` op in the push (same ordering, new sync vocabulary). Accepted cost: a device offline at the lapse keeps the item, and nothing asks again | Accepted |
| [066](ADR-066_The_Shopping_List_Is_A_Module_That_Projects_Packing_Rows.md) | **The shopping list is a module with its own entries that projects the packing list's buy rows**: `shopping_entries` holds what nobody packs, buy-mode packing rows reach M6 as `ShoppingLine`s through `lib/shoppingSources.ts` bound in `App.vue`, and `module-boundary-gate.mjs` holds the boundary — over a shopping-only flag on trip items (every packing figure must exclude it) and over copying buy rows into the list (two truths to reconcile offline). Accepted cost: M6 loses the shared composer, and entries are not in the portable backup | Accepted |
| [067](ADR-067_The_Store_Gets_A_Migration_Path_Inside_0x.md) | **The store gets a migration path inside the 0.x line**: `schema.sql` stays the readable baseline a fresh database is built from, an additive `internal/store/migrations/NNN_*.sql` chain carries an existing one forward, `PRAGMA user_version` names the applied level again, and a CI gate proves the two agree — over ADR-018's planned full revert (the schema stops being readable anywhere), over hand-migrating the live instance until 1.0 (the fingerprint is a claim, not a check) and over declaring the portable export the upgrade path (a reset: progress, photos and template links do not travel). Accepted cost: two edits per schema change, and the gate is the only thing standing between that and drift. Mechanism owed, not built | Accepted |
| [068](ADR-068_One_Task_List_For_A_Trip_With_The_Row_Named_On_The_Task.md) | **One task list for a trip, with the row named on the task**: `tripTasks` projects FR-7.4's trip todos and FR-7.3's preparations into one ordered list that M4's section, M4's figure and M1's card all read, a preparation carrying the chip of the row it prepares and leading to it — over keeping the two lists side by side (the split was the complaint) and over grouping the joined list by row (the trip's own chores would need an invented group, and a heading per row outgrows a phone). Accepted cost: one checkbox writing through two actions, a preparation still deleted only in M5, and two e2e ids now promising something different | Accepted |

**Outbound Content Fetching** — `Vision_NorthStar` names this as the gate that must exist before any Plan-phase feature
makes the server fetch external content. It had informally reserved number 007, which Session Brokering has since taken;
it gets the next free number when it is written.

## When an ADR is owed

Write one when **alternatives were genuinely weighed and one was chosen at a cost** — when a competent person could have
decided otherwise and would need to know why we didn't.

Do **not** write one for an additive config field, an endpoint that follows an existing pattern, or a mechanical
refactor. An ADR that records a non-decision dilutes the ones that matter; a sentence in the relevant spec is enough
there.

The ADR ships in the **same PR** as the code that implements it. An ADR written afterwards is a rationalisation, and one
written without code is a plan.

## Claiming a number

The next free number is a shared id that two branches will race for. Take it as late as you can, and **re-check it after
merging `main` in** — if another PR claimed it meanwhile, renumber yours (filename, heading, and every reference) before
merge.

## Structure

Follow [`TEMPLATE.md`](TEMPLATE.md), which mirrors what ADR-001 through 006 already do:

1. **Decision drivers**, in priority order — what the decision optimises for.
2. **Considered options**, each with honest pros *and* cons. An option with no cons was not considered.
3. **Decision matrix** — the drivers weighted against the options, so the conclusion is reproducible rather than
   asserted.
4. **Consequences** — what this costs us, not just what it buys.
5. **Revisit trigger** — the concrete, observable condition under which this decision should be re-opened. "If it
   becomes a problem" is not a trigger; "if item photos need to exceed ~150 KB" is.
