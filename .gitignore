# Binaries
/jitpackd
*.exe
*.dll
*.so
*.dylib

# Test output
*.test
*.out
cover.html
coverage.txt

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build
/dist/
/bin/

# SQLite databases (runtime)
*.db
*.db-wal
*.db-shm

# Environment
.env
.env.*

# MCP config (contains API keys)
.mcp.json

# Dependencies
node_modules/

# Per-feature git worktrees (the working agreement puts them here); the
# committed parts of .claude/ — commands, skills, settings.json — stay tracked.
.claude/worktrees/
.claude/settings.local.json

# Playwright output
client/playwright-report/
client/test-results/

# mkdocs build output (docs.yml builds it in CI; local `mkdocs build` too)
site/
